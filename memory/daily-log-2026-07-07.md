---
name: daily-log-2026-07-07
description: "July 7 session — Lamp site favicon + admin pagination bug fixed, FB marketing playbook built, Acme Sign site fully rebuilt (gallery-heavy, 10-task plan), deployed live, footer bugs fixed."
metadata: 
  node_type: memory
  type: project
  originSessionId: f6dc3afb-5824-4659-b1c5-17027084c3fe
---

# Daily Log — July 7, 2026

## Acme Vintage Supply (lamp site)
- Fixed favicon showing generic icon in Google (duplicate/conflicting metadata cleaned up, apple-icon.png added, stale size mismatch fixed)
- Fixed admin Products page bug: `getAdminProducts()` was hardcapped at 50/100 results with no pagination, silently hiding the 51st+ product from the admin table (storefront/inventory were unaffected). Now paginates via Shopify's GraphQL cursor.
- Built `Marketing-Claude/FACEBOOK-GROUPS-PLAYBOOK.md` — practical posting guide for the 27 FB groups, split by group type (collector vs garage-sale), since Scott has no marketing background and Peter can't join groups himself (PH-based account gets geo-blocked by local groups).
- Walked through Meta Business Suite free scheduling setup; drafted and helped publish first FB post for Acme Vintage Supply.
- Rebuilt `app/opengraph-image.tsx` — replaced text-only "business card" style link-preview image with a real product-photo mosaic (oil lamps, glass shade, burner, signs).

## Acme Sign & Graphics (acmesign.ca revamp)
- **Found current acmesign.ca is compromised**: homepage returns 404, spam content injected (gambling link + fake finance blog posts) — classic hacked-WordPress signature. WordPress admin login also locked out by Wordfence (brute-force lockout, likely from the ongoing attack). Helped draft emails to Scott about this and about needing him to follow up with the original developer (Evan Mugford / PeachBlitz).
- **Discovered `Acme-Sign-Assets/acmesign-project`** — a third, much more advanced prototype of the acmesign.ca redesign (separate from the Sanity-based `final-acmesign/acme-sign-redesign`, which should now be treated as an abandoned branch). This one already uses all 271 real converted job photos (no Unsplash placeholders), has a full home page, services index, and detailed FRONTEND/BACKEND_IMPLEMENTATION.md specs written from real acmesign.ca copy.
- Updated both spec docs with new phases: gallery-heavy content strategy, performance hardening, SEO/GEO (LocalBusiness schema + full service-area list), and backend security hardening (no backend exists yet, so that phase is deferred).
- **Executed a 10-task subagent-driven-development plan** in an isolated worktree: category-images helper (reads real photos from `public/images/{category}/` at build time), fixed 3 services showing duplicate photos, built a `RelatedWork` gallery+lightbox component wired into every service page, built the full `/gallery` page (236 real photos, category filters, pagination, lightbox), standalone `/contact` page, dedicated `/services/led-signs` and `/services/vehicle-wraps` pages, a performance audit (found already-compliant, no changes needed), and SEO/GEO (`sitemap.ts`, `robots.ts`, `LocalBusiness` JSON-LD). Final whole-branch review caught one real gap (the `?category=` deep-link filter on "View All" buttons was never read by the gallery page) — fixed before merge.
- Merged clean to `master`, worktree cleaned up.
- Added `vercel.json` (security headers). Fixed a real React warning in `Header.tsx` (setState-in-effect anti-pattern, fixed using React's "adjust state during render" pattern). Verified mobile responsiveness (all new pages tested at 375px, no issues).
- **Created GitHub repo `FearCleevan/acmesign`, pushed code. Deployed to Vercel — live at https://acme-sign.vercel.app/** (Peter deployed, not yet shown to Scott — deliberately pacing the reveal, same pattern as Frasco).
- Replaced default Next.js placeholder favicon with the real ACME "AS" monogram (`icon.png`, `apple-icon.png`, `favicon.ico` all regenerated from `acme-AS.webp`).
- Fixed two footer bugs: logo was using `brightness-0 invert` which flattened the black/red logo into an unreadable white blob (removed the filter); watermark "ACME SIGN" background text was clipped at the top (container height was 0.55em against a 22vw font-size — increased to 16vw).
- Reviewed `frasco-sage.vercel.app` (new) vs `frasco.ca` (old, also has spam/blog compromise like acmesign.ca did) — new version is clearly better designed. Frasco is Scott's friend's separate business, not being shared with anyone yet — internal comparison only.

## Key state for next session
- acmesign.ca domain is NOT yet connected to the new site — still on the compromised WordPress, DNS delegated to AWS Route 53 (not GoDaddy directly). Domain cutover is a deliberately separate, later step.
- New Acme Sign site has no backend yet (contact form is UI-only, no Supabase/Resend wired up) — that's a separate future plan.
- `acmesign-project` local repo is on `master`, pushed to `https://github.com/FearCleevan/acmesign.git`, deployed to Vercel at `https://acme-sign.vercel.app/`.
