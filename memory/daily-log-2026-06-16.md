---
name: daily-log-2026-06-16
description: "Daily log for June 16, 2026 — all work done across CLI session and Claude Code chat in one place"
metadata: 
  node_type: memory
  type: project
  originSessionId: bdcf8adf-14ea-4194-b64d-64bf7ad5ab79
---

# Daily Log — June 16, 2026

Combined log of all work done today across both the **CLI session** (Claude Code chat in terminal) and the **Claude Code desktop/web chat**.

---

## Session 1 — Admin Auth Fixes + OLC Content (CLI)

### Admin Login & Password Reset — All Bugs Fixed

| Fix | Details |
|---|---|
| `ADMIN_PASSWORD_HASH` empty on Vercel | Fixed and redeployed. Real bcrypt hash in place. |
| `ADMIN_EMAIL` empty on Vercel | Fixed to 3 recipients: jonathan, scottsfi, acmesign01 |
| `.env.local` hash mangled | bcrypt `$` signs require single quotes — `ADMIN_PASSWORD_HASH='$2b$12$...'` |
| Forgot password email matching | Was comparing against full comma-separated string — fixed to `.split(',').includes(email)` |
| 429 rate limit on localhost | Cleared Redis keys via Upstash REST API |
| Password reset didn't persist | Route was only `console.log`-ing — rewrote to write new bcrypt hash to Redis `acme:admin:password_hash` |
| No eye icons on reset form | Added `BiShow`/`BiHide` toggles on both fields |
| No real-time match validation | `useEffect([password, confirm])` shows "Passwords do not match." as user types |
| No strength indicator | Weak/Medium/Strong bar (red/amber/green) based on character type count + length |
| Remember Me missing | Checkbox on login → `rememberMe` stored in `pendingOtps` → OTP route sets `maxAge: 604800` (7 days) |

**Files changed:** `lib/admin/auth.ts`, `app/api/admin/auth/route.ts`, `app/api/admin/auth/otp/route.ts`, `app/api/admin/auth/reset/route.ts`, `app/api/admin/auth/forgot/route.ts`, `app/admin/reset-password/page.tsx`, `app/admin/login/page.tsx`

### Our Story + Heritage — Real OLC Backstory

Replaced all fabricated placeholder content (Pune/Patel/Bradley & Hubbard) on `/our-story` and `/heritage` with real backstory from Alison at the Oil Lamp Company, Melbourne:

- `data/heritage.json` — 7 real entries: Victorian golden age → Birmingham tooling → Melbourne production → India glassware → 2026 North America launch
- `data/story.json` — 3 real pillars: Original tooling / Borosilicate glass / Honesty over marketing
- `components/heritage/HeritageHero.tsx`, `WorkshopSection.tsx` — all Pune/Patel references removed
- `app/our-story/page.tsx`, `app/heritage/page.tsx` — updated metadata, testimonial replaced with real OLC quote

---

## Session 2 — Homepage & Storefront Overhaul (Claude Code chat)

### Search — Wired to Real Shopify Products

- `app/api/search/route.ts` — NEW: calls `getAllProducts()`, returns slim product data, 60s CDN cache
- `components/search/SearchOverlay.tsx` — complete rewrite: removed `mockProducts`, fetches `/api/search` on first open, result cached (re-opening instant), loading skeleton while fetching
- **Fixed:** Zodiac Ball and all Shopify-only products now appear in search; product links use real slugs (no more 404s)

### Mock Data Removed from Storefront

- `PickedOffTheBench.tsx` — removed `mockProducts` import, fallback `[]`
- All storefront sections now use real Shopify API data only

### RelatedProducts — 5 Products, Uniform Grid

- `components/product/RelatedProducts.tsx` — `slice(0, 5)`, all cards `aspectRatio="4/5"`, grid `grid-cols-2 sm:grid-cols-3 lg:grid-cols-5`

### PickedOffTheBench — 5 Products

- `lib/shopify.ts` — `getFeaturedProducts` bumped `first: 3` → `first: 5`
- Grid: `grid-cols-2 sm:grid-cols-3 lg:grid-cols-5`
- 5th card: `hidden sm:block` to avoid orphan on mobile 2-col grid

### CategoryGrid — Complete Bento Redesign

Complete rewrite of `components/home/CategoryGrid.tsx` — aspect-ratio tiles replaced with fixed-height bento grid using `Image fill` + `object-cover`.

| Breakpoint | Layout |
|---|---|
| Desktop (≥ lg) | 3-col, 2 rows × 350px. Row 1: Lighting (col-span-2) + Glass. Row 2: Burners + Signs + CTA tile. |
| Tablet (sm–lg) | 2-col, 3 rows × 260px. Lighting full-width, Glass+Burners, Signs full-width. CTA hidden. |
| Mobile (< sm) | 2-col, 3 rows × 200px. Same as tablet. |

**Local images used:**
- `01-antique-brass-oil-lamps.png` → Lighting Fixtures
- `02-green-etched-glass-oil-lamp-shade.png` → Glass Shades & Chimneys
- `03-brass-antique-lamp-burner.png` → Burners & Wicks
- `04-vintage-enamel-signs-collection.png` → Reproduction Signs
- `05-50+See-all-catalog.png` → CTA "Shop Now" tile (desktop only, dark overlay)

### CategoryRows — New Homepage Section

- `components/home/CategoryRows.tsx` — NEW async server component
- 3 rows: Oil Lamp Shades / Glass & Chimneys / Burners & Wicks
- `Promise.all` parallel fetch via `getProductsByCategory()`, `slice(0, 5)` each, empty rows hidden
- Each row: label + "View all →" link + 5 ProductCards at `aspectRatio="4/5"` in `grid-cols-5`
- Mobile: horizontal scroll via `overflow-x-auto` + `min-w-225` (900px) — all 5 cards stay full-size
- Inserted in `app/page.tsx` between `<PickedOffTheBench />` and `<TestimonialsWrapper />`

### ProductCard — Title Capped at 2 Lines

- `components/catalog/ProductCard.tsx` — added `line-clamp-2` to product name `<h3>`
- Applies everywhere: bench, category rows, related products, catalog grid

### Mobile / Tablet Fixes

- **CategoryGrid:** Responsive row heights via Tailwind grid-rows classes
- **CategoryRows:** Inner grid `min-w-225` (was `min-w-[680px]`) — 5 cards each ~180px on mobile
- **PickedOffTheBench:** Padding reduced `py-24` → `py-14 md:py-24`, 5th card hidden on mobile

---

## Session 3 — CMS Sub-project 3: Story & Heritage (CLI, this session)

### Admin Story & Heritage Editor — Complete

Found that `app/admin/content/story/page.tsx` was already fully built (two tabs, CRUD, image upload, Redis wiring) but unreachable — no sub-navigation existed between the three content admin sections.

| What | File | Change |
|---|---|---|
| Content sub-nav | `app/admin/content/layout.tsx` | NEW — Home Page / Story & Heritage / Footer Pages tabs using `usePathname()` |
| Double padding | `app/admin/content/story/page.tsx` | Removed `p-6` (admin shell already provides padding) |
| Stale defaults | `app/admin/content/story/page.tsx` | Updated `STORY_DEFAULTS` + `HERITAGE_DEFAULTS` to real OLC content |

**Result:** `/admin/content/story` is now reachable from the sidebar Content link via sub-nav tabs. Scott can edit the Our Story headline/intro/pillars and Heritage Timeline entries directly.

---

## Security Action Still Pending

**Upstash Redis token was exposed in PR #8286955.** Must be rotated:
1. upstash.com → `acme-cms` database → reset token
2. Update `UPSTASH_REDIS_REST_TOKEN` in `.env.local`
3. Update in Vercel → redeploy

---

## What's Next

1. **Rotate Upstash Redis token** — SECURITY (above)
2. **23 without-image products** — add via Shopify API (CSV: `products-new-only-without-image.csv` on Desktop)
3. **Upload photos** — Scott uploads to Shopify Admin for the 11 products added June 9
4. **Google Analytics** — Scott creates GA4 property → provides Measurement ID
5. **Business email** — `scott@acmevintagesupply.ca` via M365 (purchased, not activated)
6. **Order number branding** — Scott sets `ACME` prefix in Shopify Admin
7. **Print invoice + shipping label redesign** — waiting on Scott's sample format
