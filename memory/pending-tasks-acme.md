---
name: pending-tasks-acme
description: "Running list of confirmed 'do next shift' tasks for Acme Vintage Supply — surface and check off when user says 'Acme Recap'"
metadata: 
  node_type: memory
  type: project
  originSessionId: 083ac544-4aa9-42c5-a915-3df1b329db46
---

# Acme Vintage Supply — Next Shift To-Dos

Check this list whenever the user says "Acme Recap." Remove items once completed and confirmed by the user; add new ones whenever the user explicitly defers a task to a future session.

## Open

- **Shopify Invoice template — same domain check as Packing Slip.** The Packing Slip template's `{{ shop.domain }}` bug was found and fixed (see below), but the Invoice template (same location: Settings → Shipping and delivery → Templates) was flagged as worth the identical check and never confirmed either way. Quick look next time in Shopify settings.
- **Enamel sign pricing — still awaiting Scott's confirmation.** Enamel advertising signs listed as "Original Victorian" — need Scott to confirm genuine antique vs. reproduction so pricing can be finalized correctly (flagged 2026-07-10, still open as of 2026-07-11 daily log).

## Recently completed

- **Bot filtering + user-agent storage on page views.** ✅ COMPLETE (2026-07-14) — Vercel's raw request logs showed bot/crawler hits (e.g. an unexplained `/legal/terms` visit) with no way to tell them apart from real visitors in the actual analytics. `page_views` (the table behind the Traffic/Visitor Location dashboards) had zero bot filtering and didn't retain the user-agent. Migration `015_pageview_user_agent.sql` (run by user) adds a nullable `user_agent` column; `app/api/track/pageview/route.ts` now skips the database write entirely for known bot signatures (cheaper than before, not more expensive — no client-side/`PageViewTracker.tsx` changes, no navigation impact) and stores the raw user-agent for everything else so an ambiguous visit can be checked directly. Migration confirmed run successfully; code change ready to commit/deploy.

- **51k lead list from Brisk CRM — advised against using it.** User has 51,000 US/Canada leads obtained secondhand ("got it from someone I know, don't know how he got it") and wanted to promo-email a throttled 50/day to avoid spam filters. Flagged clearly: throttling solves deliverability, not consent — CASL (Canada's Anti-Spam Legislation) applies regardless of send rate, and an unknown-provenance list has no verifiable consent chain. Also flagged shared sending-domain reputation risk (same Resend account handles OTP/order emails). **User agreed not to use this list.** Recommended growing the real opted-in subscriber list instead, or legitimate paid channels. If this list resurfaces in a future session, the same concerns apply — don't build import/send tooling for it without re-confirming provenance.

- **"Inquire about this item" per-product contact form.** ✅ COMPLETE and confirmed live (2026-07-14). Implemented via subagent-driven-development against approved spec (`acme-lamp-sign/docs/superpowers/specs/2026-07-14-inquire-about-item-design.md`) and plan (`acme-lamp-sign/docs/superpowers/plans/2026-07-14-inquire-about-item.md`). Adds a collapsible inline form to every product detail page (Name/Email/Message, server-generated subject `Product inquiry: {title}`), reusing the existing `/api/contact` route (extended with optional `product_handle`/`product_title`, migration `014_contact_product_reference.sql`) and the Contact Inbox/rich-reply pipeline built 2026-07-11 — no new route, no new table. Admin Contact Inbox tags matching rows with a blue "Product inquiry: {title}" badge. Verified end-to-end live: inquiry on a no-price product → admin inbox badge → rich-text reply → real email received correctly on both ends.

- **Zero-price products showing "$0.00" — fixed, plus blocked accidental free checkout.** ✅ COMPLETE (2026-07-14) — Admin Dashboard's "Needs Attention" filter confirms **21 of 53 products have no price, 29 have no image** (chimney/glass items, added 2026-06-18, never finished). The $0.00 ones were displaying literal "$0.00" instead of a real price, and could actually be added to cart and checked out for free from both the catalog card and PDP. Fix is a live condition check (price === 0 / no image), not a hardcoded list, so it covers all 21/29 automatically. Now shows "Price coming soon" (and "Image & Price coming soon" combined when both are missing) in `components/catalog/ProductCard.tsx` and `components/product/ProductInfo.tsx`/`ProductGallery.tsx`, and every add-to-cart path is disabled while price is $0 (PDP shows "Not yet available for purchase" in place of the Add to Crate button). Also fixed a layout bug where the "Price coming soon" text (sized for a full price string) wrapped into/overlapped the "View details" button — now compact sans-serif text in its own row for the no-price case only.

- **Marketing — manual subscriber management + per-campaign recipient targeting.** ✅ COMPLETE and confirmed live (2026-07-14). Implemented via subagent-driven-development against approved spec (`acme-lamp-sign/docs/superpowers/specs/2026-07-11-marketing-manual-subscribers-design.md`) and plan (`acme-lamp-sign/docs/superpowers/plans/2026-07-11-marketing-manual-subscribers.md`). Subscribers tab: "Add Subscriber" (pick from existing Shopify customers via search, or manual email entry) + inline Active/Inactive toggle (reuses the existing `unsubscribed_at` field, no new column). Campaigns compose panel: "All active subscribers" vs. "Specific subscribers" targeting, persisted per-campaign via new `recipient_emails` column (migration `013_campaign_recipients.sql`) so both immediate sends and scheduled drafts respect the same choice, re-checked against active status at actual send time either way.

- **Guest cart activity tracking + display fixes.** ✅ COMPLETE (2026-07-11) — anonymous (not-logged-in) shoppers who add to cart now show up in the admin Customers list as "Guest" rows (city + cart contents), and automatically merge into the real customer record if that same browser later logs in. New `acme_visitor_id` cookie, migration `012_guest_cart_activity.sql` (run by user), generalized `lib/cartActivity.ts` identity handling. Also fixed the In Cart column to stack all cart items on separate lines instead of truncating to "+N more". `npx tsc --noEmit` and `npm run build` both clean.
- **Currency conversion using stale fallback rates — fixed.** ✅ COMPLETE (2026-07-11) — the exchange-rate fetch ran client-side directly against Frankfurter, which blocks browser requests via CORS; every lookup silently failed and fell back to hardcoded approximate rates, so displayed USD/EUR/GBP prices were never actually live. Proxied through a new server-side route (`app/api/exchange-rates/route.ts`). Confirmed checkout pricing was never affected by this bug — Shopify Markets handles the real charge independent of this display-only conversion.
- **Mobile nav hamburger menu covered by currency dropdown — fixed.** ✅ COMPLETE (2026-07-11) — currency switcher was competing for space with the hamburger on mobile/tablet widths, pushing it off-screen. Hidden from the top bar below `lg`, added into the mobile drawer's header row instead.
- **AdminTopbar z-index stacking bug — fixed.** ✅ COMPLETE (2026-07-11) — header's `z-20` was lower than the Product edit page's sticky action bar (`z-30`), so the header's search/notification/profile dropdowns rendered underneath it due to stacking-context rules. Raised header to `z-40`.
- **Packing Slip — fixed myshopify.com domain in footer (Shopify's native template).** ✅ COMPLETE (2026-07-10) — user found and fixed it directly in Shopify Admin under Settings → Shipping and delivery → Templates → Packing Slip, replacing `{{ shop.domain }}` with the literal `acmevintagesupply.com` in the footer. Also checked: Order Printer app isn't installed on this store; the native Shopify template editor lives under Shipping and delivery, not Apps; "Shipping labels" in the Shopify sidebar is the carrier-generated label system (Canada Post/UPS/etc.), not an editable template — no domain issue possible there.

- **Dashboard-Native Order Documents.** ✅ COMPLETE and confirmed live (2026-07-11) — verified visually against real order ACMEORDER-1020 (3 line items, 2 variants), all correct: Packing Slip, Pick List, and restyled Invoice all render as designed, Shipping Label unchanged. Implemented 2026-07-10 via subagent-driven-development against approved spec (`acme-lamp-sign/docs/superpowers/specs/2026-07-10-dashboard-native-order-documents-design.md`) and plan (`acme-lamp-sign/docs/superpowers/plans/2026-07-10-dashboard-native-order-documents.md`). Adds **Packing Slip** and **Pick List** print buttons/layouts to `app/admin/orders/[id]/page.tsx`, restyles the existing **Invoice** (Georgia serif → sans-serif, thick rules, corrected email) to match. **Note for future reference:** the Orders *list* page (`/admin/orders`) filters to `created_at >= LAUNCH_DATE` ('2026-07-07' in `lib/admin/shopifyAdmin.ts:14`) and will show "0 total orders" until a genuine post-launch customer order arrives — this is intentional, not a bug. The order *detail* page (`getAdminOrderById`) has no such filter, so any pre-launch order remains directly viewable/testable at `/admin/orders/{number}`. Context: step one of a larger goal — Scott should eventually run his whole order lifecycle (products, orders, tracking, fulfillment docs, notifications, support) from the Admin Dashboard alone, never touching Shopify Admin directly.

- **Contact Inbox — in-app rich text reply via Resend.** ✅ COMPLETE and confirmed live (2026-07-11). Implemented via subagent-driven-development against approved spec (`acme-lamp-sign/docs/superpowers/specs/2026-07-11-contact-inbox-rich-reply-design.md`) and plan (`acme-lamp-sign/docs/superpowers/plans/2026-07-11-contact-inbox-rich-reply.md`). Replaced the broken `mailto:` reply link (unreliable without a registered OS mail handler — this is what surfaced the whole thread) with a Tiptap-based composer supporting bold/italic/underline/lists/images/catalog product links, sending real emails via Resend and storing the sent HTML in a new `reply_body` column. **Post-deploy bug fixed same session:** `isomorphic-dompurify` (used to sanitize reply HTML) pulled in `jsdom`, which hit an ESM/CommonJS bundling conflict under Vercel's Turbopack, crashing every send with a 500. Swapped to `sanitize-html` (pure JS, no jsdom) with an explicit tag/attribute allowlist — confirmed working live via a real test reply (200 response, persisted and displayed correctly after reload).

- **Admin login OTP — fixed "expired" errors on Vercel.** ✅ COMPLETE and confirmed live (2026-07-11) — Scott replied "That worked!" after retrying login. Root cause: the OTP verification code was stored in an in-memory `Map` (`lib/admin/auth.ts`), which doesn't persist across Vercel's serverless instances — a login request and the following verify/resend request landing on different instances meant the code was simply gone, not actually expired. This is what blocked Scott from logging in. Moved to Redis (same `Redis.fromEnv()`-style pattern already used in `lib/admin/ratelimit.ts`), fixed a related bug where the incremented attempt counter wasn't being persisted, and replaced the masked-email display (which always showed Peter's own address, first in the `ADMIN_EMAIL` list, not the logging-in user's) with a generic "if your email is associated with this account..." message.

- **Notification Bell — Supabase Sources upgrade.** ✅ COMPLETE (2026-07-10) — committed and deployed, confirmed live. Implemented via subagent-driven-development against the approved spec (`acme-lamp-sign/docs/superpowers/specs/2026-07-10-notification-bell-supabase-sources-design.md`) and plan (`acme-lamp-sign/docs/superpowers/plans/2026-07-10-notification-bell-supabase-sources.md`). Adds `contact_message`, `pending_review`, `restock_signup` to `getAdminNotifications()` in `lib/admin/shopifyAdmin.ts` (+ `NotificationType` union in `lib/admin/types.ts` + icon/color mapping in `components/admin/layout/AdminTopbar.tsx`). All 3 tasks reviewed clean, `npx tsc --noEmit` zero errors, verified end-to-end against production Supabase before deploy.
