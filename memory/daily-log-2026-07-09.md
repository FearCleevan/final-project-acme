---
name: daily-log-2026-07-09
description: "July 9 session — Cart Activity Tracking feature shipped, Visitor Location Map shipped, Vercel build fix (react-simple-maps/React 19 peer-dep via .npmrc), first real customer inquiry (Allan Jeffery, Duplex chimney)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 8e03d74c-51c1-430c-af17-35823940a9fc
---

# Daily Log — July 9, 2026

## Record corrections (carried from July 8 evening)
- Access to acmesign.ca is currently restricted by prior developer Evan Mugford (PeachBlitz) — an access/handover matter. See [[project-acmesign-ca]] and [[feedback-no-hack-language]].
- Confirmed acme-sign.vercel.app (Acme Sign revamp) has already been shown to Scott.
- Google Merchant Center's 10 pending products are now confirmed approved.

## 1. Cart Activity Tracking — shipped
Full cycle: brainstorm → design spec → 11-task plan → subagent-driven-development execution → final whole-branch review (Opus) caught two real bugs (colour-variant product_id never matching the webhook's conversion check; upsert clobbering converted-order history on re-add) → both fixed and re-verified → merged to `develop` (fast-forward, `ed6cd57..1c876a3`).

Tracks what logged-in customers (never anonymous) add to cart; shows "In Cart" column on admin Customers table + "Cart Activity" section on customer detail page; converts to `converted` status via the existing `orders/paid` webhook, preserving purchase history rather than overwriting it.

**Pending:** migration `009_cart_activity.sql` needs confirmation it's actually been applied live in Supabase (mixed evidence within the session — a smoke test succeeded once, but a later independent check in the visitor-map work found no cart_activity table in the live schema).

Also: while testing the webhook conversion (Task 7), a fabricated test order (`#TEST7001`) triggered a REAL admin alert email to Scott's actual inbox (not a Shopify order, no real DB row) — a correction email draft was written for Peter to send.

## 2. Visitor Location Map — shipped
Same full cycle: brainstorm (4 clarifying questions: scope, dot granularity, placement, time range, rendering library) → design spec → 7-task plan → subagent-driven-development → final whole-branch review (Opus) found 1 Critical (deploy-ordering hazard, see below) + 1 Important (locations could be undefined during rolling deploy → fixed with `locations = []` default) → merged to `develop` (merge commit).

Captures city/lat/lng per visitor from Vercel's free edge geo headers (`x-vercel-ip-city/latitude/longitude`), aggregates by city over 30 days, renders a world map with bubbles sized by √(count) using `react-simple-maps` + `d3-geo` + locally-bundled `world-atlas` topojson (new deps, installed with `--legacy-peer-deps` since this project runs React 19.2.4 and react-simple-maps' peer deps cap at React 18 — deliberate, pre-researched override).

**CRITICAL pending:** migration `010_visitor_geo.sql` MUST be run in Supabase Dashboard SQL Editor before/with the next deploy. If the code deploys first, every `page_views` insert fails outright (Postgres rejects the whole insert for unknown columns) — this would silently break ALL pageview tracking site-wide (Top Products, Top Pages, Recent Visitors), not just the new map, since the route doesn't check the insert's error. [[project-acme-lamp-sign]]

**Session hiccup:** one implementer subagent hit a session/API rate limit mid-Task-3 and left an orphaned commit (right content, wrong parent) — recovered via `git cherry-pick` rather than redoing the work.

## 3. Production Vercel build was broken — fixed
Deploying triggered `npm error ERESOLVE` on Vercel: `react-simple-maps@3.0.0` peer deps cap at React 18, project runs React 19.2.4. Locally this was worked around by hand with `--legacy-peer-deps` on the CLI, but that never applied to Vercel's own `npm install`. Root-caused via systematic-debugging skill; fixed by adding a permanent `.npmrc` with `legacy-peer-deps=true`. Verified via a full clean `node_modules` delete + plain `npm install` (no CLI flags) matching Vercel's exact build behavior — succeeded. Committed as `92e45fa`.

Pushed and deployed (per Peter, 2026-07-09) — all of today's work (cart-activity-tracking merge, visitor-map merge, npmrc fix) is live.

## 4. First real customer inquiry
Allan Jeffery (ajeffery.ea@gmail.com, Ottawa ON, registered June 8 with 0 orders — the same "warm lead" test case that motivated Item 1/2's whole feature) sent a genuine Contact Inbox message asking for a Duplex oil lamp chimney, referencing UK supplier oldflames-lamparts.co.uk (permanently closed — owner passed away). A reply draft + a "here's what happened" update draft for Scott were both prepared. Pending: check inventory for a matching chimney before replying.

**Feature gap noticed:** Contact Inbox messages don't currently trigger a notification-bell entry (only "new customer" does) — requested as tomorrow's task, not built yet.

## 5. Process note
User asked to stop adding "Co-Authored-By: Claude Sonnet 5" to commit messages — saved as [[feedback-no-coauthor-trailer]]. Note: subagents add this by default in their own commits; the controller needs to explicitly instruct against it per-dispatch, and/or `git commit --amend` to strip it if one slips through (safe pre-merge, on local unpushed commits).
